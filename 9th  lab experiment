Aim

To study and implement basic functions of Python’s `re` module for pattern matching and string operations.



##  Algorithm

1. Start the program
2. Import the `re` module
3. Define a sample string
4. Use `match()` to check pattern at the beginning
5. Use `search()` to find a pattern in the string
6. Use `findall()` to get all matches
7. Use `finditer()` to display matches with position
8. Use `sub()` to replace characters
9. Use `split()` to split the string
10. Use `compile()` to create a reusable pattern
11. Take date input and validate using `fullmatch()`
12. Stop the program

CODE:

# Data dictionary
data = {
    "Days": ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"],
    "Temperature": [30, 12, 31, 29, 28, 27, 26] # Adjusted to match a 7-day week
}

df = pd.DataFrame(data)

plt.plot(df["Days"], df["Temperature"], marker='o', linestyle='--', color='b')

plt.xlabel("Days")
plt.ylabel("Temperature (°C)")
plt.title("Temperature over a week")
plt.grid(True)
plt.show()
Output:
  Days  Temperature
0  Mon           30
1  Tue           12
2  Wed           31
3  Thu           29
4  Fri           28
5  Sat           27
6  Sun           26

